# NTDF 简化开发计划
# 生成时间: 2026-02-22

## 一、当前问题总结

### 服务器环境问题
- ❌ apt-get命令未找到（基础环境未配置）
- ❌ 部署工具缺失（docker-compose未安装）
- ❌ SSH连接困难
- ❌ 系统环境不完整

### 可用的资源
- ✅ 腾讯云服务器（122.51.142.248）
- ✅ Nginx已安装并运行
- ✅ PostgreSQL已安装并运行
- ✅ Python 3.11已安装
- ✅ 腾讯云WebShell可用

## 二、简化开发方案

### 方案A：利用现有环境（推荐）

**不需要额外安装，直接开发**

#### 第1步：后端简化部署（今天）

**使用：**
- 腾讯云WebShell
- 现有环境：Python 3.11、PostgreSQL、Nginx
- 可用端口：8000（后端）、80（前端）

**创建后端API：**
- 简化的市场数据接口
- 简化的SR支撑压力识别
- 基础的信号识别
- 简化的用户系统

**第2步：前端简化部署（明天）**

**方案2.1：本地开发，最后部署**
- 在本地电脑开发Vue3项目
- 完成后上传到服务器
- 或使用其他云开发环境

**方案2.2：腾讯云开发环境**
- 使用腾讯云开发者工具
- 在线编辑器
- 实时预览

### 方案B：等待环境修复后部署（备选）

**需要修复的问题：**
1. apt-get命令
2. docker-compose安装
3. SSH密钥配置

## 三、简化版MVP功能

### 核心1：数据获取和展示

**数据源：**
- Alpha Vantage API（25次/天）
- Yahoo Finance API（免费）

**功能：**
- 实时报价显示
- 历史K线数据
- 成交量数据

### 核心2：简化版SR识别

**功能：**
- 波峰波谷识别
- 支撑线自动绘制
- 压力线自动绘制
- 黄金率位显示

**简化：**
- 手动调整窗口大小
- 不自动识别

### 核心3：简化版信号识别

**功能：**
- 价格突破信号
- 价格跌破信号
- 顶背离信号
- 底背离信号

**简化：**
- 手动设置阈值
- 不自动计算

### 核心4：用户系统

**功能：**
- 用户注册/登录
- 简化权限管理
- 收藏自选标的

## 四、开发时间表（简化版）

### 第1周：环境确认和数据对接

**卞董需要做的：**
1. 在腾讯云WebShell中检查环境
2. 截图告诉我：
   - Nginx是否正常运行？
   - PostgreSQL是否正常运行？
   - Python 3.11是否可用？
3. 提供目标市场（A股/港股/美股）
   - 告诉优先级（功能排序）

**我会做的：**
1. 创建简化版后端API
2. 创建基础数据模型
3. 对接Alpha Vantage API
4. 对接Yahoo Finance API
5. 实现基础SR识别算法
6. 实现基础信号识别

### 第2-3周：前端开发和基础功能

**我会做的：**
1. 创建Vue3项目
2. 实现K线图组件
3. 实现SR支撑压力图
4. 实现信号标记
5. 实现简单的数据面板
6. 实现基础的用户界面

### 第4周：测试和优化

**我会做的：**
1. 测试所有功能
2. 修复Bug
3. 优化性能
4. 编写使用文档
5. 部署到服务器（或提供部署指南）

## 五、卞董，请告诉我

### 关于当前环境

**请使用腾讯云WebShell，检查以下内容：**

1. **检查Nginx状态**
```bash
sudo systemctl status nginx
```

2. **检查PostgreSQL状态**
```bash
sudo systemctl status postgresql
```

3. **检查Python版本**
```bash
python3 --version
```

4. **检查开放端口**
```bash
sudo netstat -tuln | grep LISTEN
```

### 关于优先级

**请告诉我：**
1. **优先功能排序**（按重要程度排）
   - [ ] K线图
   - [ ] SR支撑压力
   - [ ] 信号识别
   - [ ] 数据面板
   - [ ] 用户系统

2. **目标市场**
   - [ ] A股（中国股票）
   - [ ] 港股（香港股票）
   - [ ] 美股（美国股票）

3. **其他需求**
   - [ ] 其他功能需求
   - [ ] 特定的股票代码
   - [ ] 技术栈偏好

## 六、如果环境正常，立即开始开发

### 后端开发（第1周）

**技术栈：**
- FastAPI
- PostgreSQL
- SQLModel
- Requests

**API接口：**
1. `GET /api/health` - 健康检查
2. `GET /api/market/alpha_vantage/quote?symbol=`
3. `GET /api/market/yahoo/quote?symbol=`
4. `GET /api/technical/sr?symbol=`
5. `POST /api/indicators/sma?symbol=&period=20`
6. `POST /api/indicators/ema?symbol=&period=20`

### 前端开发（第2-3周）

**技术栈：**
- Vue3 + TypeScript
- ECharts 5.x
- Axios
- Pinia

**核心组件：**
1. `Dashboard.vue` - 数据面板
2. `CandlestickChart.vue` - K线图
3. `SRChart.vue` - SR支撑压力图
4. `SignalPanel.vue` - 信号面板

## 七、紧急备用方案

### 如果环境无法使用

### 方案A：本地开发

**在我的开发环境开发**
- 代码完成后
- 提供完整的部署指南
- 您可以选择：
  - 部署到腾讯云
  - 部署到其他云服务器
  - 托管到自己的服务器

### 方案B：先开发数据接口

**只做后端API**
- 在本地开发环境开发
- 我会提供完整的API文档
- 您可以用其他方式访问API

## 八、卞董，现在需要您的反馈

### 请告诉我：

1. **当前环境检查结果**
   - Nginx是否正常运行？
   - PostgreSQL是否正常运行？
   - Python 3.11是否可用？

2. **功能优先级排序**
   - 第一优先：______
   - 第二优先：______
   - 第三优先：______

3. **目标市场**
   - [ ] A股
   - [ ] 港股
   - [ ] 美股

4. **其他需求**
   - ________

---

卞董，服务器环境有问题，我准备了简化方案。请告诉我您的反馈，我会立即开始！
